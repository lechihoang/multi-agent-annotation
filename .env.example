# NVIDIA NIM API Keys (comma-separated for multi-key pooling)
# Single key: NVIDIA_API_KEY=nvapi-xxx
# Multiple keys: NVIDIA_API_KEYS=nvapi-key1,nvapi-key2,nvapi-key3
# More keys = higher throughput (each key has 40 RPM limit)
NVIDIA_API_KEYS=your_keys_here
